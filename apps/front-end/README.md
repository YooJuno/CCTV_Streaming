# CCTV Front End

React + Vite front-end with WebRTC player and optional HLS player.

## Run (Vite)

```bash
cd apps/front-end
npm install
npm run dev
```

Open `http://localhost:5173` in the browser.
