// Copy this file to wifi_secrets.h and fill in your own credentials.
// wifi_secrets.h is git-ignored and must never be committed.
#pragma once

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
